#!/usr/bin/env bash


#--------------
# commands 
#--------------

# basename : strip the directory info and only give provide filename
#
# dirname  : strip the filename and give the directory path
#
# realpath - gives you full path for a file and dir or of any string inside a file.
#
# sleep : to create delay between 2 executions ex: sleep 1s/1m
#
# exit : to stop script if the file doesn't exit or or to just stop a script
#
# exit status ($?) : gives you status of previous command if that was successful or not

